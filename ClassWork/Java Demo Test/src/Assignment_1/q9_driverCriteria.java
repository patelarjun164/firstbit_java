package Assignment_1;

public class q9_driverCriteria {
    public static void main(String[] args) {
        int age = 50;
        int exp = 12;
        int marks = 70;

        System.out.println((age >40 && exp > 10 && marks > 60) ? "Driver is eligible" : "Driver is not eligible");
//        (age >40 && exp > 10 && marks > 60) ? printf("Driver is eligible") :printf("Driver is NOT eligible");
    }
}
